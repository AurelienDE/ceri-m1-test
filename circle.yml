machine:
  java:
    version: openjdk8
general:
  branches:
    only:
      - master # list of branches to build

      
test :
  override:
    - mvn -DprojectToken=%Project_Token% -DapiToken=%API_Token% clean test 
    - mvn jacoco:report codacy:coverage
 

 